# Backend base URL embedded into the client bundle at build time.
# Local dev (docker compose): http://localhost:8000
# Production (e.g. Vercel: https://<your-backend>.onrender.com)
NEXT_PUBLIC_API_URL=http://localhost:8000
