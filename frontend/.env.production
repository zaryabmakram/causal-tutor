# Loaded automatically by Next.js during `next build` and `next start`.
# These defaults apply to every production environment (Vercel, Render, Docker)
# unless overridden by the platform's dashboard env settings.

# Disable Next.js's anonymous build/runtime telemetry pings.
# https://nextjs.org/telemetry
NEXT_TELEMETRY_DISABLED=1
